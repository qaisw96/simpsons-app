<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partial/head.ejs')  %> 
</head>
<body>
    <header>
        <%- include('../partial/header.ejs')  %> 

    </header>

    <main>
        <% quotes.forEach(quote => { %>
            <section>

                <p>
                    <%= quote.quote %> 
                </p>
                <p>
                    <%= quote.character %> 
                </p>
                <img src="<%= quote.image %> " alt="">
                <p>
                    <%= quote.characterdirection %> 
                </p>
    
                <form action="/favorite-quotes/<%= quote.id %>?_method=DELETE" method="POST">
                    <input type="submit" va>
                </form>
    
                <form action="/favorite-quotes/<%= quote.id %>?_method=PUT " method="POST">
                    <input type="text" name="quote" value="<%=quote.quote  %> ">
                    <input type="text" name="character" value="<%=quote.character  %> ">
                    <input type="text" name="image" value="<%=quote.image  %> ">
                    <input type="text" name="characterdirection" value="<%=quote.characterdirection  %> ">
        
                    <input type="submit" value="edit">
                </form>
            </section>
        <%}) %> 
    </main>
    
</body>
</html>